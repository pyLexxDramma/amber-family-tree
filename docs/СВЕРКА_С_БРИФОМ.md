# Сверка прототипа Angelo с Angelo_project_brief.md

**Источник ТЗ:** `d:\AI_codding\Доки\Angelo\Angelo_project_brief.md`  
**Дата сверки:** 18 февраля 2026

---

## Идея и проблема

| В брифe | В проекте |
|--------|-----------|
| Закрытая семейная соцсеть; фото, видео, аудио, истории, дерево; «должно жить вечно» | ✅ Прототип: дерево, лента публикаций, медиа, профили. Legacy Storage — в брифe как тема исследования (Фаза 1), не в коде. |

---

## Платформа

| В брифe | В проекте |
|--------|-----------|
| **Telegram Mini App** (React, WebView). Абстракция от Telegram SDK — возможность переноса в PWA без переписывания. | ✅ Есть `PlatformAdapter`, `TelegramAdapter`, `MockWebAdapter`, `PlatformContext`; приложение не завязано на SDK. |

---

## Как это должно выглядеть

| В брифe | В проекте |
|--------|-----------|
| **Magazine-style галерея.** Не лента соцсети. Большие фото, подписи, как в семейном журнале. Уютно, тепло. | ✅ Стиль заложен: палитра (primary, accent, тёплые фоны), типографика (Playfair, editorial-*), карточки 60/40, лента и режим «медиа». Добавлены тёплые градиенты и акценты в AiShell и Welcome. |

---

## Функциональность — что входит в демо

### Доступ и роли

| В брифe | В проекте |
|--------|-----------|
| Закрытая сеть по приглашению; роли **Family Admin** и **Family Member**; регистрация **через Telegram** (автоидентификация). | ⚠️ Роли есть в моках (admin/member). Регистрация реализована как **телефон/email + код** (UI по макету), без реальной отправки кода и без привязки к Telegram. |

### Загрузка контента

| В брифe | В проекте |
|--------|-----------|
| Фото, видео, аудио; описания и даты; мультиселект; лимиты **20 / 500 / 100 MB**; хранение в оригинале. | ✅ Выбор типа публикации, форма с полями и датами, проверка размера файлов (20/500/100 MB), превью. Бэкенд и реальное хранение — мок. |

### Gallery Mode

| В брифe | В проекте |
|--------|-----------|
| Основной экран — визуальная галерея; большие фото + подписи (кто, когда, что); приятная навигация. | ✅ Лента в двух режимах (публикации / медиа), крупные карточки, сортировка, фильтр по тегу. |

### Семейное дерево

| В брифe | В проекте |
|--------|-----------|
| Визуализация дерева, интерактивность; клик по человеку → его фото, видео, истории. | ✅ Дерево по поколениям, карточки участников; клик ведёт в профиль контакта (публикации/медиа по контакту — в доработке по REPORT_TZ). |

### Таймлайн

| В брифe | В проекте |
|--------|-----------|
| Хронологическая лента всей семьи; фильтрация по члену семьи; видно, что происходило у разных людей в одно время. | ✅ Лента с сортировкой по дате; фильтр по тегу реализован; фильтры по автору/участникам/датам — в списке доработок (REPORT_TZ). |

### AI-функции (входят в демо по брифу)

| В брифe | В проекте |
|--------|-----------|
| **Speech-to-text (Whisper)** — голос → текст | ✅ Сценарий на странице `/ai` (мок); в AiShell на `/` — голосовой ввод через Web Speech API (демо). |
| **Стилизация текста** — разговорная речь → читаемый текст | ✅ Отдельный сценарий на `/ai` (мок). |
| **AI-ассистент** — чат-бот, навигация (найти фото, перейти в раздел) | ✅ Реализован в AiShell: чат + роутинг интентов (дерево, персона, лента, медиа, создать публикацию, помощь). Опционально LLM (OpenAI) с function calling. |
| **Анимация фото** — базовое «оживление» снимков | ✅ Сценарий на `/ai` (мок). |

---

## Что НЕ входит в демо (по брифу)

| В брифe | В проекте |
|--------|-----------|
| **Голосовой ассистент "Angelo"** — голосовые команды типа «Angelo, покажи фото Сергея за 2024 год» | ⚠️ Частично выходит за рамки «демо»: на главной странице `/` сделан голосовой интерфейс (Angelo): микрофон, распознавание, интенты, ответы. Команды вроде «покажи дерево», «расскажи про бабушку», «что нового» работают; «фото за 2024» — не реализовано. Можно считать расширением AI-ассистента с голосом. |
| Остальное (генерация видео, face recognition, параллельные таймлайны, родительский контроль, push, пакетное скачивание) | Не реализовано, как и указано в брифe. |

---

## Legacy Storage

| В брифe | В проекте |
|--------|-----------|
| Ключевая тема: данные не должны пропасть. Исследование в Фазе 1: blockchain, P2P, IPFS, шифрование, восстановление. | Не входит в разработку прототипа; это задача фазы исследования. |

---

## Тестовый контент для демо

| В брифe | В проекте |
|--------|-----------|
| 5 членов семьи (3 поколения), 50–100 фото, 10–20 видео, 5–10 аудио, дерево 15–20 человек. | ✅ В моках: 15+ членов семьи, несколько поколений, публикации с фото/видео/аудио, дерево. |

---

## Фазы по брифу

| Фаза | В брифe | В проекте |
|------|--------|-----------|
| **Фаза 1** | Архитектура, исследование legacy, анализ макетов, CJM, ER, выбор стека. | Соответствует отчётам и архитектуре (Lovable, React, платформа, моки). |
| **Фаза 2** | Вёрстка, frontend+backend, авторизация, загрузка медиа, Gallery, дерево, таймлайн, AI (Whisper, стилизация, ассистент, анимация фото). | Прототип закрывает большую часть: экраны по макету, загрузка с лимитами, лента, дерево, таймлайн, AI-сценарии. Долг: реальная авторизация, расширение фильтров, доработка экранов Семья/профиль/настройки/приглашения/места (см. REPORT_TZ.md). |

---

## Итог сверки

- **Платформа, стиль, загрузка контента, лимиты, Gallery, дерево, таймлайн, тестовый контент** — соответствуют брифу.
- **Регистрация:** в брифe — через Telegram; в прототипе — телефон/email + код (по макету). Для полного соответствия брифу нужна интеграция с Telegram или явное согласование с заказчиком.
- **AI:** все четыре пункта (STT, стилизация, ассистент, анимация фото) представлены; ассистент усилен голосовым интерфейсом на главной (Angelo), что по брифу отнесено к «будущим версиям», но логично считаться расширением демо-ассистента.
- **Детальные расхождения по экранам** (фильтры, Семья, профиль контакта, настройки, приглашения, места, видимость публикаций) перечислены в `docs/REPORT_TZ.md`.

При необходимости обновлять сверку при изменении брифa или функциональности.
